<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="viewModel"
            type="com.fraz.dartlog.viewmodel.ProfileViewModel"/>
    </data>

    <android.support.v4.widget.NestedScrollView
        style="?android:attr/textAppearanceLarge"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"
        tools:context="com.fraz.dartlog.statistics.ProfileDetailFragment">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/profile_detail_linear_layout"
            android:orientation="vertical">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:background="@color/main_white">

                <include
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    layout="@layout/profile_match_summary"
                    app:viewModel="@{viewModel}"
                    android:visibility="@{viewModel.finishedLoadingSummary ? View.VISIBLE : View.GONE}">
                </include>

                <include
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    layout="@layout/circular_progress_bar"
                    app:viewModel="@{viewModel}"
                    android:layout_centerInParent="true"
                    android:visibility="@{viewModel.showSummaryProgressBar ? View.VISIBLE : View.GONE}">
                </include>

            </RelativeLayout>

            <include layout="@layout/match_statistics_header"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="1dp"
                android:id="@+id/profile_detail_fewest_turns_label"
                app:header="@{@string/fewest_turns}"/>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:id="@+id/fewest_turns_linear_layout"
                android:orientation="vertical">

                <include
                    layout="@layout/linear_progress_bar"
                    android:visibility="@{viewModel.showHighScoresProgressBar ? View.VISIBLE : View.GONE}">
                </include>
            </LinearLayout>

            <include layout="@layout/match_statistics_header"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="1dp"
                app:header="@{@string/highest_out}"
                android:id="@+id/profile_detail_highest_out_label"/>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:id="@+id/highest_out_linear_layout"
                android:orientation="vertical">

                <include
                    layout="@layout/linear_progress_bar"
                    android:visibility="@{viewModel.showHighScoresProgressBar ? View.VISIBLE : View.GONE}">
                </include>
            </LinearLayout>
        </LinearLayout>
    </android.support.v4.widget.NestedScrollView>
</layout>