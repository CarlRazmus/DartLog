<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context=".game.settings.PlayerSelectionActivity"
    android:background="@color/background">


    <TextView
        android:id="@+id/players_listview_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Players to join this game"
        android:textAlignment="center"
        android:layout_marginTop="70dp"  />

    <View
        android:id="@+id/add_players_group_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/players_listview_header"
        android:background="@color/main_red"/>

    <Button
        android:id="@+id/ready_button"
        android:layout_width="match_parent"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="START!"/>

    <View
        android:id="@+id/ready_button_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_above="@id/ready_button"
        android:layout_marginBottom="50dp"
        android:background="@color/main_red"/>

    <RelativeLayout
        android:id="@+id/players_listview_layout_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/add_players_group_divider"
        android:layout_above="@id/ready_button_divider">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/participants_recycler_view"
            android:scrollbars="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/open_player_selection_fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/participants_recycler_view"
            android:layout_alignRight="@id/participants_recycler_view"
            android:layout_margin="10dp"
            android:clickable="true"
            app:backgroundTint="@color/main_red"
            android:src="@drawable/ic_group_add_white_24dp"/>
    </RelativeLayout>



</RelativeLayout>
